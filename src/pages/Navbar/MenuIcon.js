import styled from 'styled-components';
import MenuIcon from '@material-ui/icons/Menu';

const ZenuIcon = styled(MenuIcon)`
  color: '#fff !important';
  fill: '#fff !important';
  background: 'red';
`;

export default ZenuIcon;
