import React from 'react';
import PropTypes from 'prop-types';

function Select(props) {
  const pickedValue = props
  return (
    <Select>
      {content}
    </Select>
  );
}

Select.propTypes = {};

export default Select;
